<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/home_category_video_image"
        android:layout_width="match_parent"
        android:layout_height="230dp"
        android:layout_gravity="center_vertical"
        android:background="@drawable/round_box"
        android:padding="10dp"
        android:scaleType="fitXY"
        android:src="@drawable/sample1"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/home_category_video_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginHorizontal="20dp"
        android:text="동영상 제목이다!!!!"
        android:textSize="20sp"
        android:textStyle="bold"
        app:layout_constraintStart_toStartOf="@+id/home_category_video_image"
        app:layout_constraintTop_toBottomOf="@+id/home_category_video_image" />
</androidx.constraintlayout.widget.ConstraintLayout>